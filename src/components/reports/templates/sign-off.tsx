import { PageFooter, type ReportMeta } from "./report-shell";

export function SignOffPage({
  meta,
  startPage,
}: {
  meta: ReportMeta;
  startPage: number;
}) {
  return (
    <div className="page">
      <h2>Sign-Off</h2>
      <div className="text-sm text-muted" style={{ marginBottom: 24 }}>
        This report has been prepared using evidence submitted through SiteProof.
        All parties should review and sign below to confirm acceptance.
      </div>

      {/* Signature blocks */}
      <div style={{ display: "flex", flexDirection: "column", gap: 28 }}>
        <SignatureBlock
          role="Contractor"
          description="I confirm that the progress described in this report is accurate and supported by the evidence provided."
        />
        <SignatureBlock
          role="Project Manager"
          description="I have reviewed the evidence and progress claimed in this report and confirm it is consistent with site observations."
        />
        <SignatureBlock
          role="Client / Employer's Agent"
          description="I acknowledge receipt of this progress report and the evidence contained herein."
        />
      </div>

      {/* Legal disclaimer */}
      <div
        style={{
          marginTop: 40,
          padding: 16,
          background: "#f8fafc",
          borderRadius: 8,
          border: "1px solid #e2e8f0",
        }}
      >
        <h3 style={{ fontSize: 10, color: "#475569", marginBottom: 6 }}>
          Disclaimer
        </h3>
        <div style={{ fontSize: 8, color: "#64748b", lineHeight: 1.7 }}>
          <p style={{ marginBottom: 6 }}>
            This report has been automatically generated by SiteProof based on
            photographic and video evidence uploaded by site personnel. While every
            effort has been made to ensure accuracy, the report relies on the
            integrity of the submitted evidence including EXIF metadata, GPS
            coordinates, and timestamps embedded in the original media files.
          </p>
          <p style={{ marginBottom: 6 }}>
            SiteProof does not independently verify the physical progress on site.
            The GPS coordinates and timestamps are extracted from device metadata
            and have not been independently verified unless explicitly stated in the
            Verification section of this report.
          </p>
          <p style={{ marginBottom: 6 }}>
            This document is confidential and intended solely for the parties named
            above. Unauthorised distribution, copying, or disclosure of this report
            is prohibited. The document may be password-protected; recipients are
            responsible for maintaining the security of access credentials.
          </p>
          <p>
            Report generated on{" "}
            {new Date(meta.generatedAt).toLocaleString("en-GB", {
              day: "numeric",
              month: "long",
              year: "numeric",
              hour: "2-digit",
              minute: "2-digit",
            })}{" "}
            by SiteProof v1.0.
          </p>
        </div>
      </div>

      <PageFooter meta={meta} pageNum={startPage} />
    </div>
  );
}

function SignatureBlock({
  role,
  description,
}: {
  role: string;
  description: string;
}) {
  return (
    <div
      style={{
        border: "1px solid #e2e8f0",
        borderRadius: 8,
        padding: "16px 20px",
      }}
    >
      <div
        style={{
          fontSize: 13,
          fontWeight: 600,
          color: "#0f172a",
          marginBottom: 4,
        }}
      >
        {role}
      </div>
      <div style={{ fontSize: 10, color: "#64748b", marginBottom: 16 }}>
        {description}
      </div>

      <div style={{ display: "flex", gap: 24 }}>
        <div style={{ flex: 2 }}>
          <div style={{ fontSize: 9, color: "#94a3b8", marginBottom: 4 }}>
            Name & Title
          </div>
          <div
            style={{
              borderBottom: "1px solid #cbd5e1",
              height: 28,
            }}
          />
        </div>
        <div style={{ flex: 2 }}>
          <div style={{ fontSize: 9, color: "#94a3b8", marginBottom: 4 }}>
            Signature
          </div>
          <div
            style={{
              borderBottom: "1px solid #cbd5e1",
              height: 28,
            }}
          />
        </div>
        <div style={{ flex: 1 }}>
          <div style={{ fontSize: 9, color: "#94a3b8", marginBottom: 4 }}>
            Date
          </div>
          <div
            style={{
              borderBottom: "1px solid #cbd5e1",
              height: 28,
            }}
          />
        </div>
      </div>
    </div>
  );
}
